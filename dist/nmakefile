dirs = libxsde examples

all:
	@for %i in ( $(dirs) ) do \
@cmd /c "cd %i & $(MAKE) /nologo /f nmakefile"

test:
	@for %i in ( $(dirs) ) do \
@cmd /c "cd %i & $(MAKE) /nologo /f nmakefile $@"

gen clean cleanobj:
	@for %i in ( $(dirs) tests ) do \
@cmd /c "cd %i & $(MAKE) /nologo /f nmakefile $@"
